<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>MXL Gold | Luxury Home & Lifestyle Secrets 2026</title>
    <meta name="description" content="Discover the luxury home trends and exclusive products that elite women in New York, London, and Paris are obsessing over in 2026. Curated finds on Amazon.">
    <meta name="keywords" content="luxury home 2026, luxury lifestyle women, smart home NYC, elite home decor, luxury products Amazon, high end home essentials, Beverly Hills luxury, Manhattan apartment luxury">
    <meta name="robots" content="index, follow, max-snippet:-1, max-image-preview:large">
    <link rel="canonical" href="https://mxlgold.com/">

    <!-- Open Graph -->
    <meta property="og:type" content="website">
    <meta property="og:site_name" content="MXL Gold">
    <meta property="og:title" content="The Luxury Secrets Elite Women in NYC & London Don't Share">
    <meta property="og:description" content="Exclusive luxury trends and curated Amazon finds for high-income women in New York, London, and Paris.">
    <meta property="og:image" content="https://images.pexels.com/photos/1643383/pexels-photo-1643383.jpeg?auto=compress&cs=tinysrgb&w=1200">
    <meta property="og:image:width" content="1200">
    <meta property="og:image:height" content="630">
    <meta property="og:url" content="https://mxlgold.com/">
    <meta property="og:locale" content="en_US">

    <!-- Twitter -->
    <meta name="twitter:card" content="summary_large_image">
    <meta name="twitter:title" content="The Luxury Secrets Elite Women in NYC & London Don't Share">
    <meta name="twitter:description" content="Exclusive luxury trends and curated Amazon finds for high-income women.">
    <meta name="twitter:image" content="https://images.pexels.com/photos/1643383/pexels-photo-1643383.jpeg?auto=compress&cs=tinysrgb&w=1200">

    <!-- Schema.org -->
    <script type="application/ld+json">
    {
        "@context": "https://schema.org",
        "@type": "WebSite",
        "name": "MXL Gold",
        "url": "https://mxlgold.com/",
        "description": "Luxury lifestyle trends and exclusive Amazon products for high-income women in NYC, London, and Paris.",
        "inLanguage": "en-US",
        "publisher": { "@type": "Organization", "name": "MXL Gold" }
    }
    </script>

    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:ital,wght@0,400;0,700;1,400&family=DM+Sans:wght@300;400;500;600&display=swap" rel="stylesheet">

    <style>
        * { margin:0; padding:0; box-sizing:border-box; }
        body { font-family:'DM Sans',sans-serif; background:#fffaf7; color:#2c2418; line-height:1.6; }

        /* HEADER */
        .hero {
            background: linear-gradient(135deg,#1a1a2e 0%,#16213e 100%);
            color: white; padding: 90px 20px; text-align: center;
            position: relative; overflow: hidden;
        }
        .hero::before {
            content:''; position:absolute; inset:0;
            background: radial-gradient(ellipse at 65% 40%, rgba(255,69,0,.14) 0%, transparent 65%);
        }
        .hero-eyebrow {
            font-size:.75rem; letter-spacing:3px; text-transform:uppercase;
            color:#c9a87b; margin-bottom:20px; position:relative;
        }
        .hero h1 {
            font-family:'Playfair Display',serif;
            font-size:clamp(1.8rem,4.5vw,3.2rem); font-weight:700;
            line-height:1.2; margin-bottom:18px; position:relative;
        }
        .hero h1 em {
            font-style:italic;
            background:linear-gradient(135deg,#ff4500,#ff6b35);
            -webkit-background-clip:text; -webkit-text-fill-color:transparent;
        }
        .hero-sub { color:#a89070; font-size:.95rem; max-width:500px; margin:0 auto; position:relative; }
        .live-pill {
            display:inline-flex; align-items:center; gap:7px;
            background:rgba(255,69,0,.15); border:1px solid rgba(255,69,0,.4);
            color:#ff6b35; padding:6px 16px; border-radius:40px;
            font-size:.72rem; font-weight:700; letter-spacing:1.5px;
            text-transform:uppercase; margin-top:28px; position:relative;
        }
        .live-dot { width:6px; height:6px; border-radius:50%; background:#ff4500; animation:blink 1.4s infinite; }
        @keyframes blink { 0%,100%{opacity:1} 50%{opacity:.2} }

        /* LAYOUT */
        .container { max-width:1300px; margin:0 auto; padding:70px 20px; }
        .section + .section { margin-top:80px; }

        .section-label {
            font-size:.68rem; letter-spacing:3px; text-transform:uppercase;
            color:#c9a87b; text-align:center; margin-bottom:12px;
        }
        .section-title {
            font-family:'Playfair Display',serif; font-size:1.85rem;
            text-align:center; color:#2c2418; margin-bottom:48px;
        }
        .section-title::after {
            content:''; display:block; width:50px; height:2px;
            background:#c9a87b; margin:14px auto 0;
        }

        /* GRID */
        .grid { display:grid; grid-template-columns:repeat(auto-fill,minmax(340px,1fr)); gap:28px; }
        .grid-news { display:grid; grid-template-columns:repeat(auto-fill,minmax(280px,1fr)); gap:22px; }

        /* CARD */
        .card {
            background:white; border-radius:20px; overflow:hidden;
            box-shadow:0 4px 20px rgba(0,0,0,.06); border:1px solid #f0e2d8;
            transition:transform .3s ease, box-shadow .3s ease;
        }
        .card:hover { transform:translateY(-5px); box-shadow:0 14px 40px rgba(0,0,0,.12); }

        .card-img { width:100%; height:220px; overflow:hidden; background:#faf7f3; }
        .card-img img { width:100%; height:100%; object-fit:cover; transition:transform .4s ease; }
        .card:hover .card-img img { transform:scale(1.04); }
        .card-img.contain img { object-fit:contain; padding:20px; transform:none; }
        .card:hover .card-img.contain img { transform:scale(1.03); }

        .card-body { padding:24px; }

        .card-tag {
            display:inline-block; background:#f7ede8; color:#ff4500;
            font-size:.65rem; padding:4px 12px; border-radius:20px;
            font-weight:700; text-transform:uppercase; letter-spacing:.8px;
        }

        .card-title {
            font-family:'Playfair Display',serif; font-size:1.05rem;
            line-height:1.45; margin:10px 0 8px; color:#1a1208;
        }
        .card-text { font-size:.875rem; color:#6b5a48; line-height:1.65; }

        /* SOCIAL PROOF */
        .views-badge {
            display:inline-flex; align-items:center; gap:5px;
            font-size:.7rem; color:#b07040; margin:6px 0 0;
            font-weight:500;
        }

        /* RELATED PRODUCT BLOCK inside insight card */
        .related-block {
            margin-top:18px; padding:14px; background:#faf7f3;
            border-radius:12px; border:1px solid #ede0d4;
            display:flex; gap:12px; align-items:center;
        }
        .related-block img {
            width:58px; height:58px; object-fit:contain;
            border-radius:8px; background:white; padding:4px; flex-shrink:0;
        }
        .related-block-info { flex:1; min-width:0; }
        .related-eyebrow { font-size:.6rem; color:#c9a87b; text-transform:uppercase; letter-spacing:.8px; font-weight:700; }
        .related-name { font-size:.78rem; color:#1a1208; font-weight:600; margin:2px 0 7px; white-space:nowrap; overflow:hidden; text-overflow:ellipsis; }

        /* BUTTONS */
        .btn-primary {
            display:block; width:100%; background:#1a1208; color:white;
            padding:12px; border-radius:40px; text-align:center;
            text-decoration:none; font-weight:600; font-size:.85rem;
            margin-top:18px; border:none; cursor:pointer;
            transition:background .25s ease, transform .15s ease;
        }
        .btn-primary:hover { background:#ff4500; transform:translateY(-1px); }

        .btn-pill {
            display:inline-block; background:#ff4500; color:white;
            padding:6px 14px; border-radius:20px; font-size:.72rem;
            font-weight:700; text-decoration:none; border:none; cursor:pointer;
            transition:background .25s;
        }
        .btn-pill:hover { background:#1a1208; }

        /* NEWS CARD */
        .news-card {
            background:white; border-radius:16px; overflow:hidden;
            box-shadow:0 4px 16px rgba(0,0,0,.06); border:1px solid #f0e2d8;
            display:flex; flex-direction:column;
            transition:transform .3s;
        }
        .news-card:hover { transform:translateY(-4px); }
        .news-img { width:100%; height:155px; object-fit:cover; }
        .news-body { padding:16px; flex:1; display:flex; flex-direction:column; }
        .news-source { font-size:.6rem; color:#c9a87b; text-transform:uppercase; letter-spacing:.8px; font-weight:700; margin-bottom:6px; }
        .news-title { font-family:'Playfair Display',serif; font-size:.9rem; line-height:1.4; flex:1; color:#1a1208; }
        .news-text { font-size:.78rem; color:#6b5a48; margin-top:8px; line-height:1.6; }
        .news-link { font-size:.72rem; color:#ff4500; text-decoration:none; margin-top:10px; font-weight:700; display:inline-block; }
        .news-link:hover { color:#1a1208; }

        /* EMPTY / LOADER */
        .loader { text-align:center; padding:50px 20px; color:#c9a87b; grid-column:1/-1; font-size:.9rem; }

        /* FOOTER */
        footer {
            background:#110e08; color:#6b5a40; text-align:center;
            padding:48px 20px; margin-top:40px; font-size:.75rem; line-height:2.2;
        }
        footer a { color:#9a7a50; text-decoration:none; }
        .footer-disclaimer {
            max-width:680px; margin:16px auto 0; font-size:.68rem;
            color:#4a3a24; line-height:1.8;
        }

        @media(max-width:768px) {
            .grid, .grid-news { grid-template-columns:1fr; }
            .hero { padding:60px 20px; }
        }
    </style>
</head>
<body>

<!-- HERO -->
<header class="hero">
    <p class="hero-eyebrow">✦ The insider's guide to luxury living ✦</p>
    <h1>The Luxury Secrets<br><em>Elite Women</em> Keep to Themselves</h1>
    <p class="hero-sub">Curated for women who demand the best — from Manhattan penthouses to London townhouses.</p>
    <div class="live-pill">
        <span class="live-dot"></span>
        Live trends · Updated every 15 min
    </div>
</header>

<main class="container">

    <!-- INSIGHTS -->
    <section class="section" aria-label="Luxury lifestyle insights">
        <p class="section-label">✦ Insider Intelligence ✦</p>
        <h2 class="section-title">What They're Buying Right Now</h2>
        <div id="insightsGrid" class="grid" role="list">
            <div class="loader">Curating luxury insights...</div>
        </div>
    </section>

    <!-- PRODUCTS -->
    <section class="section" aria-label="Exclusive luxury products on Amazon">
        <p class="section-label">✦ Exclusive Finds ✦</p>
        <h2 class="section-title">Shop the Elite Edit</h2>
        <div id="productsGrid" class="grid" role="list">
            <div class="loader">Loading exclusive products...</div>
        </div>
    </section>

    <!-- NEWS -->
    <section class="section" aria-label="Luxury lifestyle news">
        <p class="section-label">✦ Latest Intelligence ✦</p>
        <h2 class="section-title">Trending This Week</h2>
        <div id="newsGrid" class="grid-news" role="list">
            <div class="loader">Loading luxury news...</div>
        </div>
    </section>

</main>

<footer>
    <p><strong style="color:#c9a87b;">MXL Gold</strong> — Luxury Lifestyle & Exclusive Finds</p>
    <p><a href="/sitemap.xml">Sitemap</a> &nbsp;·&nbsp; © 2026 MXL Gold</p>
    <p class="footer-disclaimer">
        MXL Gold is a participant in the Amazon Services LLC Associates Program, an affiliate advertising
        program designed to provide a means for sites to earn advertising fees by advertising and linking
        to Amazon.com. We may earn a commission on qualifying purchases at no additional cost to you.
    </p>
</footer>

<script>
// ── Click tracking ──
async function trackClick(productId, url) {
    try {
        await fetch('/api/track/click', {
            method: 'POST',
            headers: { 'Content-Type': 'application/json' },
            body: JSON.stringify({ producto_id: productId, tipo: 'amazon' })
        });
    } catch(e) {}
    window.open(url, '_blank', 'noopener noreferrer');
}

// ── Insights (formerly curiosidades) ──
async function loadInsights() {
    try {
        const data = await fetch('/api/curiosidades').then(r => r.json());
        const grid = document.getElementById('insightsGrid');
        if (!data?.length) {
            grid.innerHTML = '<div class="loader">Generating luxury insights with AI... check back shortly.</div>';
            return;
        }
        grid.innerHTML = data.map(c => {
            const relatedHTML = c.producto_link ? `
                <div class="related-block">
                    <img src="${c.producto_imagen || ''}"
                         alt="${c.producto_titulo || 'Related product'}" loading="lazy"
                         onerror="this.src='https://images.pexels.com/photos/280229/pexels-photo-280229.jpeg?auto=compress&cs=tinysrgb&w=200'">
                    <div class="related-block-info">
                        <div class="related-eyebrow">✨ Found on Amazon</div>
                        <div class="related-name">${c.producto_titulo || ''}</div>
                        <button class="btn-pill" onclick="trackClick(${c.producto_id_ref},'${c.producto_link}')">
                            See price →
                        </button>
                    </div>
                </div>` : '';

            return `
            <article class="card" role="listitem" itemscope itemtype="https://schema.org/Article">
                <div class="card-img">
                    <img src="${c.imagen}" alt="${c.titulo_es}" loading="lazy" itemprop="image"
                         onerror="this.src='https://images.pexels.com/photos/258154/pexels-photo-258154.jpeg?auto=compress&cs=tinysrgb&w=600'">
                </div>
                <div class="card-body">
                    <h3 class="card-title" itemprop="headline">${c.titulo_es}</h3>
                    <p class="card-text" itemprop="articleBody">${c.texto_es}</p>
                    ${relatedHTML}
                </div>
            </article>`;
        }).join('');
    } catch(e) { console.error(e); }
}

// ── Products ──
async function loadProducts() {
    try {
        const products = await fetch('/api/productos').then(r => r.json());
        const grid = document.getElementById('productsGrid');
        if (!products?.length) {
            grid.innerHTML = '<div class="loader">No products yet. Inject your first product from the MXL panel.</div>';
            return;
        }
        grid.innerHTML = products.map(p => `
            <article class="card" role="listitem" id="product-${p.id}"
                     itemscope itemtype="https://schema.org/Product">
                <div class="card-img contain">
                    <img src="${p.imagen}" alt="${p.titulo} — Luxury product" loading="lazy" itemprop="image"
                         onerror="this.src='https://images.pexels.com/photos/280229/pexels-photo-280229.jpeg?auto=compress&cs=tinysrgb&w=600'">
                </div>
                <div class="card-body">
                    <span class="card-tag">${p.categoria || 'LUXURY'}</span>
                    ${p.clics > 0 ? `<div class="views-badge">🔥 ${p.clics} people viewed this today</div>` : ''}
                    <h3 class="card-title" itemprop="name">${p.titulo}</h3>
                    <p class="card-text" itemprop="description">${p.curiosidad || ''}</p>
                    <button class="btn-primary" onclick="trackClick(${p.id},'${p.link}')" itemprop="url">
                        View on Amazon →
                    </button>
                </div>
            </article>
        `).join('');
    } catch(e) { console.error(e); }
}

// ── News ──
async function loadNews() {
    try {
        const data = await fetch('/api/noticias').then(r => r.json());
        const grid = document.getElementById('newsGrid');
        if (!data?.length) {
            grid.innerHTML = '<div class="loader">Loading luxury news...</div>';
            return;
        }
        grid.innerHTML = data.map(n => `
            <article class="news-card" role="listitem">
                <img class="news-img" src="${n.imagen}" alt="${n.titulo}" loading="lazy"
                     onerror="this.src='https://images.pexels.com/photos/1643383/pexels-photo-1643383.jpeg?auto=compress&cs=tinysrgb&w=600'">
                <div class="news-body">
                    <div class="news-source">${n.fuente}</div>
                    <h3 class="news-title">${n.titulo}</h3>
                    <p class="news-text">${n.resumen}</p>
                    ${n.link ? `<a href="${n.link}" class="news-link" target="_blank" rel="noopener">Read more →</a>` : ''}
                </div>
            </article>
        `).join('');
    } catch(e) { console.error(e); }
}

// Init
loadInsights();
loadProducts();
loadNews();
setInterval(() => { loadInsights(); loadProducts(); loadNews(); }, 60000);
</script>
</body>
</html>
